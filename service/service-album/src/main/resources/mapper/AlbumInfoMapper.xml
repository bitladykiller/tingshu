<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.atguigu.tingshu.album.mapper.AlbumInfoMapper">

    <select id="selectAlbumPage" resultType="com.atguigu.tingshu.vo.album.AlbumInfoVo">
        select
        album.id as albumId,
        album_title,
        album.cover_url,
        album.include_track_count,
        album.is_finished,
        max(if(stat.stat_type = '0401', stat.stat_num, 0)) as playStatNum,
        max(if(stat.stat_type = '0402', stat.stat_num, 0)) as subscribeStatNum,
        max(if(stat.stat_type = '0403', stat.stat_num, 0)) as buyStatNum,
        max(if(stat.stat_type = '0404', stat.stat_num, 0)) as commentStatNum
        from album_info album inner join album_stat stat on album.id = stat.album_id
        <where>
            <if test="vo.userId!=null">
                album.user_id = #{vo.userId}
            </if>
            <if test="vo.status!=null and vo.status!=''">
                and album.status = #{vo.status}
            </if>
            <if test="vo.albumTitle!=null and vo.albumTitle!=''">
                and album.album_title like concat('%',#{vo.albumTitle},'%')
            </if>
            and album.is_deleted = 0
        </where>
        GROUP BY album.id
        order by album.id desc
    </select>
    <select id="selectAlbumStat" resultType="com.atguigu.tingshu.vo.album.AlbumStatVo">
        select stat.album_id,
               max(if(stat.stat_type = '0401', stat.stat_num, 0)) playStatNum,
               max(if(stat.stat_type = '0402', stat.stat_num, 0)) subscribeStatNum,
               max(if(stat.stat_type = '0403', stat.stat_num, 0)) buyStatNum,
               max(if(stat.stat_type = '0404', stat.stat_num, 0)) commentStatNum
        from album_stat stat
        where album_id = #{albumId}
          and stat.is_deleted = 0
        group by stat.album_id
    </select>


</mapper>

